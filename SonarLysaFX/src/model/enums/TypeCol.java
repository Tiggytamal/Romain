package model.enums;

import java.io.Serializable;

public enum TypeCol implements Serializable, TypeKey
{
    DIRECTION, DEPARTEMENT, SERVICE, RESPSERVICE, CLARITY, LIBELLE, CPI, EDITION, 
    LOT, ENV, ANOMALIE, ETAT, SECURITE, REMARQUE, VERSION, DATECREATION, DATERELANCE, 
    TRAITE, FILIERE, MANAGER, MATIERE;
}
